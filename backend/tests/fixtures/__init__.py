"""Golden synthetic fixtures for F0 (master §6).

Small, hand-authored, **decoupled from the C corpus** so unit tests never depend on DATA-C. A Wave-1
session extends the relevant fixture (never weakens it) as it adds behaviour. See ``loaders`` for the
programmatic accessors the gates + tests use.
"""
