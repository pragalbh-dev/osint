"""RK-BAKEOFF spec gates (test-author hand, implementation-blind).

Written from artifacts/plan/01-replumb-implementation-plan.md §8 before/independent of the
implementation under ``backend/eval/extraction/``. Files are ``test_*_spec.py`` so both hands'
takes survive integration (precedent: tests/gates/test_g15_presence_not_fused_spec.py).
"""
