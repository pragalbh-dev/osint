# Backend build + runtime artifacts — never committed.
*.egg-info/
.data/            # runtime SQLite copy (settings.data_dir)
.venv/
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# coverage artifacts
.coverage
htmlcov/
