# 12 — Build log: autonomous data session (2026-07-17)

**Window:** overnight autonomous run (you asleep, reviewing ~6h later). **Objective (your brief):** figure out
a concrete misinformation-planting strategy reusable across A/B/C, get + generate as much data as possible,
write the requirements you owe me, leave scripts + strategy ready. Key is now `ANTHROPIC_API_KEY`.

---

## What was built

| Deliverable | Where |
|---|---|
| **Data-generation strategy** — reusable engine + 11-operator misinformation plan + corroboration/refutability design + bypass suite + observable realignment + A/B reuse | `artifacts/md/10-data-generation-strategy.md` |
| **Requirements from you** — decisions (A1–A6) + prioritized logins (Copernicus, Google Earth, SIPRI export, residential-IP run) | `artifacts/md/11-data-requirements-from-pragalbh.md` |
| **Operator catalogue** — enumerated corruption + deception operators (signal earned-in-text, detector, expected behaviour) | `tools/generate/operators.yaml` |
| **Generator v2** — operator injection, source-registry + expected-behaviour answer key, concurrency, chaff mode, `ANTHROPIC_API_KEY` | `tools/generate/generate.py` |
| **Signal scenario** — realigned to Rawalpindi→Rahwali observable; audit gaps fixed | `tools/generate/scenarios/hq9p_primary.yaml` (23 docs) |
| **Chaff scenario** — echoes / distractors / stale / 2nd spoof | `tools/generate/scenarios/hq9p_chaff.yaml` (27 docs) |
| **Frozen corpus** — 50 raw docs + answer keys | `corpus/scenarios/{hq9p_primary,hq9p_chaff}/docs/` |

## Corpus state

- **50 docs** = 23 signal + 27 chaff (was 14) — top of `09-corpus-sizing`'s ~40–50 band (S≈20/N≈20–30).
- **Realigned to the LOCKED observable** (HQ-9B Rawalpindi→Rahwali 2025): 4-doc thread (2021 baseline →
  2025 single-pass *probable* via decoy cap → 2nd-independent *confirmed* → supersedes retires Rawalpindi
  to stale) + the D8 supersede-spoof.
- **Audit gaps fixed** (from `06-preflight-audit`): edge-connected worked-query path (H-CONSIST-1);
  relocation seed data (H-CONSIST-2); flagship `distinct-from` HQ-9/P vs HQ-9BE + `same-as` FD-2000→HQ-9/P +
  FT-2000 `distinct-from` (H-CONSIST-3); HT-233 = **candidate** chokepoint + `substitutability: UNKNOWN` +
  Known Gap (M-INCONSIST-1); d05 cover-story left unresolved (M-DATA-1); imagery = analyst-report **text**
  (H-DEVIATION-1); clean ontology type strings (L-CONSIST-1).
- **Deception coverage:** all 11 operators instantiated. **Bypass suite** live: aggregator (D3, d01),
  aligned-interest (D4, d02+d15), adversary-denial (D5, d16), **supersede-spoof (D8, d20 + cx01)** — the
  last beats even the current design and ships with its fix (a confidence-floor on `supersedes`; approved
  by default, demo both ways).
- Every doc's `answer_key.json` entry carries the ground-truth source-registry (`reliability_grade`,
  `bias_vector`, `primary_origin_id`, `aggregator_of`, `first_seen`, `decoy_risk_flag`,
  `coordinated_inauthenticity_flag`, `adversary_denial_flag`) + `expect` (the verification oracle). These
  are what the pipeline must **compute** from the raw text — they are NOT in the docs.

## Verification (adversarial)

Ran a 41-checker adversarial workflow: does each doc *earn* its deception signal from the raw text, play
its role, and **not** state the verdict? **Result: 3 major + 3 minor, all fixed.** All six were the same
failure mode — the model appended a self-debunking analyst note that spelled out the answer ("not to be
confused with…", "traces back to one origin", "as of 2017 still limited to…"). Fix: strengthened the
generator's no-self-debunking constraint + made `cd07` genuinely off-subject; regenerated the six;
re-scanned clean. (One doc, `d19`, tripped a keyword scan on the words "decoy"/"deception" used in
legitimate analyst prose — a false positive, left as-is because it's realistic.)

The corpus was then extended from 41 → **50** with **9 more off-subject distractors** (batch-2 depth:
Turkey/Saudi/HQ-22/civilian-trade/academic/NOTAM/name-collision/rumor/budget). These 9 were phrase-scanned
clean (no self-debunking, no on-subject leak; `cd14_china_hq22` mentions HQ-9 only as a real HQ-22 article
would — the proximity decoy working) but were **not** put through the full LLM verifier (plain distractors
have no deception op to earn; the scan covers what matters). Re-verify command below if you want the full
50 checked.

Residual honesty: the LLM verifier ran once over all 41; the six fixes were confirmed by targeted phrase
re-scan + spot-read, not a second full LLM pass (that would re-spend ~1.1M tokens to confirm six known
fixes). A full re-verify is one command away if you want it (below).

## Reproduce / extend

```bash
# regenerate everything (concurrent):
python tools/generate/generate.py tools/generate/scenarios/hq9p_primary.yaml --workers 6
python tools/generate/generate.py tools/generate/scenarios/hq9p_chaff.yaml   --workers 6
# dry-run coverage only:
python tools/generate/generate.py <scenario>.yaml --check
# regenerate specific docs:
python tools/generate/generate.py <scenario>.yaml --only d20_supersede_spoof
```
Retune by editing the scenario YAML (per-doc `deception` / `corruption` / `registry` / `expect`) or the
operator catalogue. A/B: add `operators.yaml` reuse + a new scenario shape (A: `baseline[]` time-series;
B: `events[]`+`inferences[]`) — no engine change (strategy §7).

## When you wake (checklist → `11-data-requirements-from-pragalbh.md`)

1. Skim `10-…` §4 (misinformation plan) + §6 (realignment). 5 min.
2. Answer **A1** (imagery posture) and **A2** (sustainment node) — one line each; they unblock the most.
3. Do **B#1–#4** (Copernicus, Google Earth, SIPRI export, residential-IP `gather.py`) when you have 20 min
   — the biggest fidelity jump (imagery is the only true data gap; SIPRI/ISPR upgrade from reconstruction
   to raw).
4. `python tools/gather/gather.py --reconcile && python tools/generate/generate.py <scenario>.yaml`.

## Addendum — imagery pipeline + deep-tier grounding (2026-07-17, cont.)

Follow-up session resolved the imagery question and the deep-tier supplier residual.

**Decisions locked:** A1 = **VLM/hybrid imagery** (real images shown in-app for every claim); A2 = **keep BOTH
sustainment nodes** (Interceptor Stockpile = observable/resupply story; Tech-Data Authority = invisible-
dependency chokepoint) — solid cause each, prioritised fetch (tenders first, inference second); A3 =
contested-classes-only; A4 = **08's noisy-OR math + add an `insufficient` state to the 08 machine** (the
data already models it via the gap flexes; the spine/08 doc edit is yours to make); A5 = D8 confidence-floor
approved; A6 = moot.

**"OMNI" = Gemini Omni** (video, `gemini-omni-flash-preview`). For stills: **Nano Banana 2/Pro**
(`gemini-3-pro-image`). Caption VLM: `gemini-3.1-pro-preview`.

**Imagery tooling (new, all validated):**
- `tools/gather/copernicus_fetch.py` — real Sentinel-2 via Copernicus Sentinel Hub (CLIENT_ID/CLIENT_SECRET
  OAuth). Pulled 4 real frames (Karachi, Karachi-cloudy, Rawalpindi 2021, Rahwali 2025).
- `tools/generate/imagery.py` — fabricated images (Nano Banana Pro) + **iterative generate→VLM-critique→
  refine** loop (keeps best-of-N) + VLM caption.
- `tools/generate/attach_images.py` — hybrid attach pass; **shared images persist** so the recycled/echo
  cluster reuses ONE identical image (verified: d11/d12/d13 + ce01/ce02/ce03 all `sha d51931db…`).
- Integrity tags in `answer_key.json` (`image.integrity: real|synthetic`) — real Sentinel confirms; fabricated
  are the deception/illustration (M4 should flag synthetic). Generator preserves image metadata across text-regen.

**Imagery posture (hybrid):** real Sentinel-2 for the confirm/observable frames (can't self-flag); fabricated
Nano-Banana for **social photos** (convoy sighting) + the **shared recycled parade photo** (the M4 star) +
labeled stand-ins. Fabricated realism scores 0.35–0.45 (critic flags AI tells) — correct: grainy = realistic
for social, and the tells are what M4 catches.

**Deep-tier supplier hunt (Q5 resolved — bounded, real):** CASIC → **2nd Academy / China Academy of Defence
Technology / China Changfeng Group** (real design authority; "CADI" was a false lead); **CPMIEC = export
agent only** (markets as FD-2000, NOT the maker); **HT-233 manufacturer = UNKNOWN** per the authoritative
CASI/BluePath 2025 study, which *debunks* the common "CPMIEC makes it" claim; best candidate = **CASIC 23rd
Research Institute (Beijing Institute of Radio Measurement)**, inferred not confirmed; CASIC 4th Academy =
plausible-not-confirmed motor supplier; deeper tiers (T/R modules, seeker, datalink) = honest gaps.
**Chokepoint = candidate (validated as not-a-defensible-sole-source in open sources).** Corpus regrounded:
`d22` now names the real entities + the Unknown; **new `d23`** = the CPMIEC-manufacturer false-attribution trap.

**Corpus now: 51 docs** (24 signal + 27 chaff), 18 graph nodes / 23 edges, **11 with images** (4 real
Sentinel + 7 fabricated).

## Addendum — location precision + normalization system (2026-07-17, cont. 2)

Closed the gap that every place was a **bare toponym in one format** — the location-normalizer had
nothing to resolve. Full spec + rationale in **`artifacts/md/13-location-normalization.md`**.

**What ORBAT actually needs (per-node-type precision), now specified:** fire-unit/`based-at` → pad/site
(the observable is a location state-change, must be confirmable in 10 m imagery); manufacturer/design-
authority → facility+city+district (collection-taskable, but a grid adds nothing we have); port →
terminal; HQ → city; unobservable → Known Gap. Precision is set by the *touching query/observable*, not
node grandeur — the relocating HQ-9B is the most precision-hungry node; the Beijing design authority needs
only district.

**Real coordinates researched (subagent, cited public OSINT)** for 6 anchor sites, in DD/DMS/MGRS/UTM +
aliases + ICAO/LOCODE. Posture: **anchor base/port coords are real & public; the Karachi SAM pad is
synthetic-from-real & tagged** (`provenance` field on every coord) — we don't publish novel battery fixes.

**≥2 surface formats per demonstrated site, seeded across independent docs** (`operators.yaml` gained
`coordinate_precise` + `toponym_alias`, the counterpoints to `coordinate_absence`):
- **Rahwali** (observable dest): `d18` DMS `32°14′20″N 074°07′52″E` ≡ `d19` "Rahwali Cantt, ~10 km NW of
  Gujranwala" — **load-bearing**: the 2nd independent signal only corroborates once both resolve to one site.
- **Nur Khan/Chaklala** (observable origin): `d17` MGRS `43S CT 23715 21242` + "Nur Khan (fmr Chaklala)"
  — MGRS parse + **renamed-base alias** ("Chaklala" **withheld** from the seed gazetteer → earned merge).
- **Port Qasim** (entry): `d05` "Bin Qasim / PQ"; **distinct-from trap** vs Port of Karachi (Keamari),
  ~35 km apart, both "Karachi" (the geographic FT-2000).
- **Karachi SAM site**: `d07` DD `24.9012 N 67.2034 E` (synthetic pad) — distinct from both ports; bare
  "Karachi" (`d02`) stays ambiguous parent-city.
- **CASIC 2nd Academy / 23rd RI (BIRM)**: `d22` facility+city+district (Yongding Rd, Haidian, Beijing).

**Wiring:** `ground_truth.places[]` gazetteer (7 nodes) + per-doc `expect.location` oracle +
`flexes.location_normalization` in `hq9p_primary.yaml`; **`config/places.yaml`** = the extensible seed
gazetteer (Chaklala alias + Rahwali relative form deliberately omitted → adaptation demo). Regenerated 6
docs (d05/d07/d17/d18/d19/d22) — all surface forms verified in raw text, image metadata preserved, self-
debunk scan clean (2 false positives = legitimate analytic prose). Re-pulled the 2 real Sentinel frames
whose coords moved (Nur Khan +5 km, Rahwali +2.4 km) → now centered on the actual airfields.

**Sentinel site coords refined** (`copernicus_fetch.py` SITES) from approximate city-centers to the real
Nur Khan (OPRN) / Rahwali airfield coords — closes the "refine before freezing" residual for those two.

## Addendum — high-res imagery tier (Esri) + real-relabeled SAM frames (2026-07-17, cont. 3)

**Problem raised (Pragalbh):** the real Sentinel-2 frames (10 m) show airfields/urban mush — no launchers,
no SAM sites; the VLM (correctly, honestly) captioned them as "airport / dense urban," supporting **no**
positive HQ-9 claim. And the doc TEXT (d07 "petal/ring SAM pad + TEL revetments") overclaimed vs its image.

**Root cause:** Sentinel-2 has no closer look (10 m is its max; a TEL is ~1 px). Verified visually.

**Fix — a sub-meter tier + real-relabeled SAM sites:**
- **`tools/gather/esri_fetch.py`** (new) — Esri World Imagery (ArcGIS export), **free, keyless, ~0.5 m**;
  aggregates Maxar/Airbus. Proven: at Nur Khan it resolves individual aircraft. `imagery.real_esri()` +
  a `source: real_esri` branch in `attach_images.py` wire it in; `attach_images` now records
  `provenance` + `real_source`.
- **Real SAM sites, verified in current imagery, relabeled to scenario sites** (posture: real unaltered
  image = `integrity: real`; the *location* is relabeled for the fabricated scenario; answer key records
  `provenance: relabeled` + `real_source` — auditable, not deceptive). Two subagents + my own visual
  verification of 9 candidate sites (most 2007-era coords were converted/urbanized/empty):
  - **d07 Karachi (confirmed HERO)** ← HQ-9 nr Xi'an (occupied petal). VLM: *"multi-lobed cleared area
    enclosed by a circular perimeter track… small white objects."* ✅
  - **d18 Rahwali (single-pass PROBABLE)** ← S-400 nr Feodosia, Crimea (occupied, dispersed). VLM:
    *"ring road encloses ~20-25 dark elongated objects in linear arrays."* The ambiguity *fits* the
    decoy-risk cap — a clean petal here would contradict "probable." ✅
  - **d17 Rawalpindi (2021 baseline)** ← PLA air-defence garrison nr Nanjing. VLM: *"circular unpaved
    track encloses buildings… barracks-like complexes."* ✅
  - **d17b forward site (NO TELs gap)** ← EMPTY HQ-9 petal nr Lanzhou. VLM: *"circular, multi-lobed
    ground pattern… rectangular foundations"* — shape present, **no launchers named** = the gap. ✅
- **Design elegance:** clear frame → confirmed claim; ambiguous frame → probable claim; empty frame →
  the gap. The imagery quality *matches* the credibility state.
- **Sentinel-2 kept** only for **d10 cloud-gap** (low-res + cloud = the intended insufficient-evidence beat).

**Corpus imagery now: 12 doc images** — 5 real (4 Esri sub-meter SAM sites + 1 Sentinel cloud-gap) + 7
fabricated (1 convoy + the 6-doc recycled-parade cluster). Fabricated frames are JPEG-encoded with a
`.png` name (Nano Banana returns JPEG) — valid images, browser-renders; convert only if a strict
extension-keyed loader needs it.

## Addendum — chaff adversarial verification + fixes (2026-07-18)

Ran the full adversarial verifier over all 27 chaff docs (the batch that was previously only phrase-scanned).
**Result: 22 PASS, 5 FAIL** — all the same failure mode as the earlier signal pass: docs *self-debunking*
the trap they're supposed to BE.
- **cd01** (S-400) said "should not be confused with Pakistan's HQ-9-series … share no common supply chain" → resolved the S-400↔HQ-9 trap.
- **cd03** (HQ-16) said "analysts caution against conflating LY-80 with HQ-9/P" AND leaked substantive HQ-9/P operator/tier claims.
- **cd04** (civ electronics) said "no association with any govt/defence procurement entity" → pre-resolved the HS-code collision.
- **cs02** (stale) announced "much of the current discussion appears to be recycling older reporting" → contradicts the stale-**as-current** masquerade; also date mismatch (artifact read 2025, YAML said 2018-04).
- **ce03** (echo) contained in-thread debunkers naming the mechanism ("same frame reused across 4-5 accounts").

**Fix:** added a first-class **`avoid`** field to the generator (`build_prompt` injects an explicit
"ABSOLUTELY DO NOT include …" prohibition) — the same trick that made cd06/cd07 pass by baking the ban into
`intent`. Set `avoid` on all 5; additionally demoted cd03's HQ-9/P to a bare passing mention and reset cs02's
dateline to **2025-05** (report_time recent, event_time 2018 embedded → decays on age, not self-admission).
Regenerated the 5; **re-scanned clean** (cd01 now 0 HQ-9 mentions; cd03 0; none self-flag). Residual honesty:
the 5 fixes were phrase-scanned + spot-read, not a 2nd full LLM verifier pass.

**Verifier cross-cutting notes (accepted as-is, flagged):** the 6-post recycled burst is byte-identical
across both scenarios (sha `d51931db…`) and captions *diverge* over the one image (a stronger test than the
"near-identical" notes imply — note wording is conservative, not wrong). `cs01` attributes HQ-9/P to the
**PAF** (real-world = Army) — left as intentional stale-era conflation + an extra resolution test (Army HQ-9/P
vs our PAF HQ-9B). Minor NOTAM realism nits (cd05 stray Honolulu line; cd16 Islamabad-area coords) left as
plausible `code_string_noise`.

## Known residuals / caveats

- ~~**Imagery is still analyst-report *text***~~ **RESOLVED** — 11 real image files now attached (4 real
  Sentinel-2 + 7 fabricated); each doc keeps its analyst-report text *and* the image evidence. The two
  observable-site frames (Nur Khan, Rahwali) are pulled at the real airfield coords. Karachi SAM-pad coord
  is synthetic (by posture, not a residual). Remaining coord refinement (if wanted): the CASIC/BIRM Beijing
  anchor is district-level approximate — that's the *correct* precision for a design-authority node (md/13),
  so it's intentional, not a gap.
- **The subject-proximity view-time filter is a required *pipeline* build stage** at this corpus size
  (`09 §5`) — not data, so I couldn't build it; distractors are low-proximity by construction so they sink
  even without it, but it's on the build path (flag to `06-preflight-audit`'s ladder).
- **Calibration constants + the 04-vs-08 resolver reconciliation** are your sign-off (A3–A5); I used the
  08-canonical noisy-OR math with 04's gate list.
- `d01` renders as a defence-journal tracker citing SIPRI (the aggregator D3 case), not a raw SIPRI CSV row
  — because the gathered SIPRI specimen is a reconstruction; your SIPRI export (B#3) upgrades it.
